name: mobx_examples
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  extensions:
    git:
      url: https://github.com/samuelematias/basic_flutter_packages
      ref: master
      path: packages/extensions
  increment_counter_package:
    path: ../increment_counter

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.2

flutter:
  uses-material-design: true
